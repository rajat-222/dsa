#include<iostream>
#include<stdlib.h>
#include<stdio.h>

using namespace std;

// struct Pointer{

// }s;

int main(){
    // int a = 10;

    // int A[5] = {2, 3, 4, 5, 6};
    // p = A;
    // int *p;

    // cout<<"using the array"<<endl;
    // for(int i = 0; i<5; i++)
    // cout<<A[i]<<" ";

    // cout<<"using the pointer"<<endl;
    // for(int i = 0; i<5; i++)
    // cout<<p[i]<<" ";
    // delete [ ] p;
    // free(p);

    // printf("pointer %d\n", *p);
    // printf("Address %d\n", p);

    // p=(int *)malloc(s*sizeof(int));
    // p=new int[s];
}